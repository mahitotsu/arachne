spring:
  application:
    name: arachne-phase3-jdbc-session
  main:
    web-application-type: none
  datasource:
    url: jdbc:h2:file:./.arachne/jdbc/phase3-session-db;AUTO_SERVER=TRUE
    username: sa
    password:
  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql

arachne:
  strands:
    agent:
      system-prompt: "Remember the latest destination across application restarts."
      conversation:
        window-size: 8
      session:
        id: phase3-jdbc-demo