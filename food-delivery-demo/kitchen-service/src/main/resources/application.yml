spring:
  application:
    name: kitchen-service
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: ${DELIVERY_JWK_SET_URI:http://customer-service:8080/oauth2/jwks}

delivery:
  registry:
    base-url: ${DELIVERY_REGISTRY_BASE_URL:}
  kitchen:
    endpoint: ${DELIVERY_KITCHEN_ENDPOINT:http://kitchen-service:8080}
  downstream:
    menu:
      capability-query: ${MENU_CAPABILITY_QUERY:欠品代替候補の準備と提案}

management:
  endpoints:
    web:
      exposure:
        include: health,info
